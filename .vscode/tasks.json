{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "compile vscode-netron",
            "type": "shell",
            "command": "npm run compile",
            "options": {
                "cwd": "${workspaceFolder}/vscode-netron"
            },
            "problemMatcher": ["$tsc"]
        }
    ]
}
